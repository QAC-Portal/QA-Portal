import { ExpanderDirective } from './expander.directive';

describe('ExpanderDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpanderDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
